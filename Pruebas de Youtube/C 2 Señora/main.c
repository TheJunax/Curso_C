#include <stdio.h>
#include <stdlib.h>
#include "header.h"
// El perucas es un mal amigo

int main() {
    printf("suma > %lf", suma(2, 6));
    printf("resta > %lf", resta(2, 6));

    return EXIT_SUCCESS;
}